# Stadrey Wilson — Resume (React + TypeScript)

A clean, responsive resume site built with React + TypeScript (Vite). Includes search, sections, and a print-to-PDF button.

## Run locally
```bash
npm install
npm run dev


















